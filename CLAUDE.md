# WORKING RULES

## RULE ONE · SEARCH BEFORE YOU ASK

**Before putting any question to Lance, search the project for the answer.**

```bash
python3 pipeline/find.py "the thing you think is missing"
```

It covers all 47 sources including the 11MB manuscript and the corpus.

This rule exists because it has already been broken twice, and both times
the material was there.

| I asked for | Where it actually was |
|---|---|
| Addresses for 371 entries | The Cluster Family Architecture in the manuscript. Clusters share one somatic address, and the families are named. Took the count from 19 to 199. |
| The generator's `compose/` logic | Written out in full inside the corpus as the 3C generator spec. |

**If the search comes back empty, say where you looked before you ask.**

---

## THE OTHER RULES

**Voice, absolute.** No em dashes, ever. Short sentences. Mechanical and
precise. No soft wellness language. Physical metaphors only. Stories carry
the authority, not credentials.

**Never interpolate a somatic address.** If it is not in canon or dictated,
it does not go in. Mark every address `OBSERVED` or `DERIVED`. Never invent
a third category.

**Node count is 112.** 108 in-body, 4 field. Ruled v214.

**Render and look at the output before claiming it works.**

**Answer in summaries.** Lead with the answer. Then what changed. Then what
is next and who owns it. Keep it short.

---

## STATE

| | |
|---|---|
| Manuscript | `index.html`, v214, 136k words |
| Generator | `generator/`, runs, builds a 32-page volume |
| Corpus | `corpus/`, 390 entries, 199 addressed |
| Books | structurally valid, prose being filled from the corpus |
| Site | `site/`, builds, Stripe wired, needs keys and a print vendor |

**Ruled:** publishing direct, not licensing. **thelittlebooksof.com is the
house** (R7). Sharing Human is secondary and not on the pipeline.

**Open, on Lance:** print vendor (Lulu or Bookvault), paperback price
($29 proposed, no canon number).

**Open, on me:** charge names are dictation fragments, see
`site/SITE_NOTES.md`. Fix before the site goes live.
