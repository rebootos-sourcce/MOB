{
  "name": "thelittlebooksof",
  "private": true,
  "version": "1.0.0",
  "description": "thelittlebooksof.com. Static site, Stripe checkout, signed PDF delivery, print on demand.",
  "scripts": {
    "build": "python3 build_site.py",
    "dev": "python3 build_site.py && python3 -m http.server 4000 --directory public"
  },
  "dependencies": {
    "stripe": "^17.0.0",
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/s3-request-presigner": "^3.700.0"
  }
}
